def cookie(x):
    if isinstance(x,str):
        name ="Zach"
    elif not isinstance(x,bool) and isinstance(x,(float,int)):
        name = "Monica"             
    else:
        name = "the dog"           
    return f"Who ate the last cookie? It was {name}!"
